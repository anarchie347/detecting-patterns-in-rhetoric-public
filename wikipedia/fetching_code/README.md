# wikipedia scraper

run `npm i` to install necessary packages

call with `node wikipedia_only.mjs <count> <delay>`

- count: #articles to fetch
- delay: delay in seconds between requests to prevent rate limiting


## Note: only 'wikipedia_only.mjs' works at the moment
